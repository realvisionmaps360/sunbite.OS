project_id = "kvqgrzfgpzpzmakiddse"

# A funcao ai-parse valida o JWT do chamador por conta propria, via
# getClaims() — ver supabase/functions/ai-parse/index.ts. Nao desligamos a
# verificacao do gateway: quem chama precisa estar logado, ponto.
[functions.ai-parse]
enabled = true
verify_jwt = true
